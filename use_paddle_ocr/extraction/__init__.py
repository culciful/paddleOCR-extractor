"""
Structured data extraction from people_llm (manifest + entries).

Add CLI modules or libraries here (e.g. LLM JSON extraction). Outputs should go under
extractions/<book>/ at repo root by convention (tracked; output/ is often gitignored).
"""
