# Structured extraction outputs (liuxuesheng)

Write downstream artifacts here: JSON/JSONL/CSV, LLM runs, evaluation logs.

**Upstream (read-only):** `output/liuxuesheng/people_llm/` (`manifest.jsonl`, `entries/*.txt`).

Keeping this tree **outside** `output/` lets it be tracked by Git while large OCR artifacts stay ignored.
